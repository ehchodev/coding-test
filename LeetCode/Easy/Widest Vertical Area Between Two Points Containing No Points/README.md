# 1637. Widest Vertical Area Between Two Points Containing No Points

**Given `n` `points` on a 2D plane where `points[i] = [xi, yi]`, Return the widest vertical area between two points such that no points are inside the area.**  
2차원 평면 위에 `points[i] = [xi, yi]`를 만족하는 `points`가 `n` 개 주어질 때, 영역 내에 어떤 점도 포함하지 않는 두 점 사이의 가장 넓은 수직 영역을 반환하라.

**A vertical area is an area of fixed-width extending infinitely along the y-axis (i.e., infinite height). The widest vertical area is the one with the maximum width.**  
수직 영역이란 y축을 따라 무한으로 확장되는 고정된 너비의 영역을 말한다(즉, 길이가 무한). 가장 넓은 수직 영역은 가장 너비가 큰 것이다.

**Note that points on the edge of a vertical area are not considered included in the area.**  
수직 영역의 경계선 위에 있는 점은 영역에 포함된 것으로 보지 않는다는 것을 유의하라.
